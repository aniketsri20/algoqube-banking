export class KeyAsInitialDefaultValuePostProcessor {
    name = 'KeyAsInitialDefaultValue';
    process(draft, extracted, existing) {
        return draft.map((key, val) => val.value === '' && !existing.has(key) ? { value: key, sourceFiles: (val?.sourceFiles || []) } : val);
    }
}
//# sourceMappingURL=key-as-initial-default-value.post-processor.js.map